<%- include('../../partials/layout/head', { page_title: 'Game Index' }) %>

<h1>All completed games for <%= title %></h1>

<div class="completed-game">
  <% games.forEach(game => { %>
    <p><%= game %></p>
  <% }) %>
</div>

<%- include('../../partials/layout/tail', { footer: 'Test footer' }) %>